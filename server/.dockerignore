# 依赖与构建产物：必须排除 node_modules（含本地编译的原生模块如 better-sqlite3，
# 若带入镜像会与容器内 npm install 的产物冲突，导致 libnode 加载失败）
node_modules/
web/node_modules/
web/dist/
web/*.timestamp-*.mjs
web/vite.config.js
web/vite.config.d.ts
# 本地运行时数据（数据库、日志等）不入镜像
data/
public.bak-*/
*.log
*.db
# 密钥文件绝不入镜像（.env 含 ADMIN_TOKEN/SESSION_SECRET/SMTP_PASS 等）；
# 否则 COPY . . 会将其烘焙进镜像层，镜像一旦推送 registry 即造成泄密。
# 保留 .env.example 供使用者参考。
.env
.env.*
!.env.example
